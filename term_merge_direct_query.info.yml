name: Term Merge by Direct SQL Queries
description: Allows users to merge taxonomy terms in the same vocabulary together.
core: 8.x
type: module
# core: 8.x
dependencies:
  - term_merge

dependencies:
 - drupal:taxonomy
 - term_reference_change:term_reference_change
